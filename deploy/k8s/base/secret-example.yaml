# EXAMPLE secret — DO NOT use in production. Replace with an
# ExternalSecret managed by External Secrets Operator backed by
# Vault / AWS Secrets Manager / GCP Secret Manager.
#
# `kubectl apply` of this file is opt-in: it's intentionally not
# referenced by kustomization.yaml's `resources` list once you switch
# to ExternalSecret. For dev/staging, fill in the values below and
# `kubectl apply -f`.

apiVersion: v1
kind: Secret
metadata:
  name: scopilot-secrets
type: Opaque
stringData:
  SCOPILOT_ANTHROPIC__API_KEY: "REPLACE_ME"
  SCOPILOT_API__API_KEYS: '["REPLACE_ME_WITH_A_TOKEN"]'
  SCOPILOT_WEBEX__BOT_ACCESS_TOKEN: ""
  SCOPILOT_WEBEX__WEBHOOK_SECRET: ""
  SCOPILOT_WEBEX__OPERATORS_ROOM_ID: ""
  SCOPILOT_THREAT__ABUSEIPDB_API_KEY: ""
  SCOPILOT_THREAT__OTX_API_KEY: ""
  SCOPILOT_THREAT__VIRUSTOTAL_API_KEY: ""
  POSTGRES_PASSWORD: "scopilot"
